from .base import BaseClient


class CustomClient(BaseClient):
    """
    An abstract subclass of BaseClass that inherits the abstract methods.
    It does not provide implementations, making it abstract as well.
    """

    pass
